name: CI with Email Step

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: 23f1000658@ds.study.iitm.ac.in - Setup Python
        run: echo "Setting up Python environment"

      - name: Test API
        run: echo "Running tests..."
